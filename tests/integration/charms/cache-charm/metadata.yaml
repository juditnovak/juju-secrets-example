# This file populates the Overview on Charmhub.
# See https://juju.is/docs/sdk/metadata-reference for a checklist and guidance.

# See https://juju.is/docs/sdk/naming#heading--naming-charms for guidance.
name: secrets-cache-charm
 
display-name: Test for Juju Secrets

summary: Test to demonstrate some behavior with juju secrets.

description: Test charm to demonstrate some juju secrets related behavior.

peers:
    charm-peer:
        interface: charm-peer

resources:
  secrets-cache-charm-image:
    type: oci-image
    upstream-source: "ghcr.io/canonical/secrets-cache-charm:0.0.1-22.04_edge"
